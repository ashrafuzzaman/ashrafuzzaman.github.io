---
import "/node_modules/reveal.js/dist/reveal.css";
import "/node_modules/reveal.js/dist/theme/solarized.css";
import "/node_modules/reveal.js/plugin/highlight/monokai.css";
---

<script>
  import Reveal from "reveal.js";
  import RevealMermaid from "reveal.js-mermaid-plugin/plugin/mermaid/mermaid.js";
  import RevealHighlight from "reveal.js/plugin/highlight/highlight.js";
  import Markdown from "reveal.js/plugin/markdown/markdown.js";

  Reveal.initialize({
    plugins: [Markdown, RevealHighlight, RevealMermaid],
    markdown: {
      smartypants: true,
    },
    mermaid: {
      securityLevel: "sandbox",
      theme: "default",
      themeCSS: `
        rect.actor { rx: 10; ry: 10; }
        rect.actor + text.actor tspan { fill: #fff !important; }
        :has(body) { display: flex !important; justify-content: center; }
      `,
      themeVariables: {
        actorBkg: "#2F7FC1",
        actorBorder: "#2F7FC1",
      },
      sequence: {
        height: 55,
        mirrorActors: false,
        actorFontWeight: 800,
      },
    },
    fragments: true,
    backgroundTransition: "slide",
  });
</script>
